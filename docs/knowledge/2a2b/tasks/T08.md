# T08 — Construcción, selección y contratos comunes

## Inicio y coordinación

- Inicio de implementación: T07 cerrado; usar clasificación y casos de T06. La lectura preparatoria puede adelantarse.
- Leer [README de coordinación](../README.md). Solo el coordinador cambia la checklist principal y asigna propiedad. Esta tarea fija la base compartida antes de T09/T10.
- Base histórica `9118ca2` en `origin/2A2B`; trabajar en HEAD asignado, preservando cambios previos.
- Propiedad candidata: `packages/python/roster-construction/`, pruebas de construcción y ampliaciones mínimas de contratos/modelos/bindings comunes. Cada archivo fuera del lote necesita asignación; el YAML canónico no queda disponible automáticamente tras T07.

## Objetivo y lecturas

Compilar y seleccionar correctamente los perfiles y opciones incorporados: límites, equipo, restricciones, decisiones iniciales y concesiones de reglas/habilidades. Reutilizar `packages/python/roster-construction/mordheim_construction/{compiler,selection,restrictions,contracts}.py`, `sources/knowledge/registry/bindings.yaml`, `tests/python/construction/` y `docs/guides/campaign-knowledge.md`.

El compilador ya aplica reglas automáticas según `runtime`, concesión y bindings; verificar los consumidores antes de introducir otra vía de aplicación. Para selección de campaña seguir también los servicios/dominio de Python y TypeScript; no confundir compilación de un luchador con una banda válida persistida.

## Pasos

- [ ] Extraer de T06 los IDs y comportamientos de construcción, con casos de activación y exclusión.
- [ ] Trazar todos los llamadores de los mecanismos afectados; identificar interfaces compartidas que necesiten T09/T10 y fijarlas primero.
- [ ] Demostrar cada carencia con un caso mínimo en la suite existente; reutilizar fixtures y dados/decisiones inyectables.
- [ ] Reutilizar el binding o efecto equivalente; ampliar en la capa común solo cuando no pueda expresar la fuente.
- [ ] Aplicar reglas por destinatario y condiciones correctas: perfil/banda, equipo seleccionado, opciones exclusivas, límites y excepciones.
- [ ] Comprobar que un efecto concedido automáticamente no se aplica de nuevo como habilidad seleccionada o regla de perfil.
- [ ] Mantener validación en dominio/servicio, además de las opciones de UI; una carga o llamada directa no debe saltarse restricciones.
- [ ] Coordinar cambios equivalentes Python/TypeScript donde exista el mismo flujo. Las nuevas elecciones deben llegar al servicio y persistencia; derivar presentación a su propietario sin dejar el flujo incompleto.
- [ ] Probar valores límite, selección válida/inválida, ausencia de regla y una interacción por mecanismo compartido modificado.
- [ ] Ejecutar `python tools/mordheim-utils.py tests --scope construction` y `python tools/mordheim-utils.py verify --structural`; ejecutar casos de campaña afectados mediante sus suites existentes.
- [ ] Entregar contrato final, archivos cambiados, mecanismos y casos al coordinador; declarar archivos liberados y cambios pendientes que impedirían T09/T10.

## Fin y evidencia

Cada efecto de construcción asignado produce resultado correcto o rechazo explícito; todas las bandas aprobadas tienen al menos una construcción/selección válida cuando la fuente lo permita. No se inventan opciones para satisfacer tests. Entregar cobertura por ID, pruebas y contratos definitivos. T09/T10 pueden iniciar implementaciones paralelas únicamente con propiedad disjunta confirmada.

## Prompt de ejecución

> Ejecuta T08 leyendo este documento, ../README.md y las entregas T06/T07. Confirma asignación de archivos. Reutiliza construcción, selección, restricciones y bindings existentes para los efectos asignados; sigue cada flujo hasta validación y consumidor. Fija primero cualquier contrato que necesiten combate y campaña. Verifica límites, concesión automática sin duplicados y equivalencia Python/TypeScript donde aplique. No implementes sistemas nuevos de batalla ni un segundo pipeline. Entrega pruebas por mecanismo y contrato estable al coordinador; no cambies la checklist principal ni hagas commit/push por iniciativa propia.

## Prompt de revisión

> Revisa T08 contra las fuentes e inventario T06. Comprueba todos los llamadores del mecanismo común y que la validación no exista solo en UI. Busca doble concesión, opciones fuera del perfil, límites mal aplicados y regresiones en bandas anteriores. Revisa evidencia de al menos una selección válida para cada banda. Señala qué contratos pueden liberarse con seguridad para T09/T10 y qué cambios los bloquean.

## Prompt de reanudación

> Retoma T08 leyendo última entrega y HEAD actual. Confirma si T09/T10 ya usan algún contrato antes de alterarlo. Recupera casos pendientes, evita duplicar efectos ya implementados, solicita asignación de archivos compartidos y valida solo cambios afectados antes de entregar el contrato actualizado al coordinador.
