# T07 — Promoción serial de la KB canónica

## Inicio y coordinación

- Inicio: T02, T03, T04, T05 y T06 cerradas y entregas revisadas.
- Esta tarea es serial: un único propietario escribe la KB completa y los contratos/pruebas de promoción. Ningún otro agente modifica esos archivos durante la operación.
- Leer [README de coordinación](../README.md); solo el coordinador actualiza la checklist principal. Obtener su asignación explícita de archivos compartidos.
- Base histórica: `9118ca2` en `origin/2A2B`; usar HEAD actual asignado y preservar cambios posteriores.

## Objetivo, referencias y propiedad

Promover todos los registros aprobados, una sola vez y con IDs estables, manteniendo staging. Entradas: mapa origen/destino editorial, previsualización y herramienta de T05, inventario T06. Propiedad prevista: `sources/knowledge/`, ajustes mínimos de `contracts/knowledge-editorial-v1/` y pruebas de carga/promoción; concretar archivos antes de escribir.

Leer `tools/ingestion/normalize_staging_for_promotion.py`, `packages/python/knowledge/mordheim_knowledge/staging_promotion.py`, `sources/2B/promotion-schema-plan.md`, `sources/2B/catalog/promotion-merge-notes.md`, `tests/python/knowledge/test_2a_staging.py`, `test_2b_staging.py` y `test_editorial_schemas.py` en ese mismo directorio.

## Pasos

- [ ] Registrar HEAD, estado y entrega T05. La opción actual `--write` del normalizador normaliza staging: no asumir que promueve. Usar exclusivamente el contrato de promoción que T05 haya implementado y probado.
- [ ] Obtener previsualización contra la revisión actual; cotejar altas, fusiones, sustituciones y eliminaciones con el inventario aprobado.
- [ ] Bloquear colisiones no resueltas, pérdida de texto y referencias ambiguas; devolver cada discrepancia al propietario editorial correspondiente.
- [ ] Promover catálogos y registros compartidos antes que bandas. Aplicar los precios generales y excepciones en sus contextos correctos.
- [ ] Incorporar bandas y referencias, manteniendo IDs canónicos existentes y la trazabilidad de IDs fusionados. No introducir un cargador que lea ambos árboles.
- [ ] Verificar el caso `lore.prayers-of-taal-and-rhya` según la decisión editorial, sin aceptar su eliminación solo porque la propone un normalizador.
- [ ] Completar la cobertura de esquemas de documentos promovidos, incluidos perfiles 2B; validar formas y referencias nuevas.
- [ ] Adaptar pruebas que prohíban coincidencias staging/KB: comprobar procedencia y carga única conservando staging. No borrar una prueba sin reemplazar su intención válida.
- [ ] Revisar cantidades fijadas en tests contra el inventario aprobado, no contra lo que casualmente devuelva el cargador.
- [ ] Ejecutar una segunda previsualización/promoción controlada para demostrar idempotencia; el segundo pase no debe añadir duplicados ni modificar contenido.
- [ ] Ejecutar pruebas de colisiones, entradas inválidas o interrupciones sobre fixtures y directorios temporales aislados. No alterar la KB compartida para provocar un fallo mientras otros agentes la leen.
- [ ] Comprobar conservación de la KB previa y campañas con referencias existentes, y registrar cualquier cambio deliberado.
- [ ] Ejecutar `python tools/mordheim-utils.py tests --scope knowledge` y `python tools/mordheim-utils.py verify --structural`; separar fallos previos de regresiones con evidencia.
- [ ] Entregar HEAD/diff y lista final de IDs; liberar propiedad para consumidores y T08. Coordinar generación web con su propietario, nunca editar JSON a mano.

## Fin y evidencia

Todos los registros aprobados se cargan exactamente una vez desde KB activa; referencias y esquemas pasan; staging permanece; la segunda operación es vacía y ninguna pérdida carece de justificación. Entregar informe antes/después, pruebas, diff de bajas y cambios a entidades previas. La promoción no acredita automatización pendiente de T08–T10.

## Prompt de ejecución

> Ejecuta T07 según este documento y ../README.md, solo tras el cierre T02–T06 y con propiedad exclusiva asignada. Revisa previsualización T05 en HEAD actual, promueve primero catálogos y después bandas mediante la herramienta existente ampliada. Mantén staging, IDs estables y una sola fuente de carga. Actualiza contratos y pruebas para promoción real, valida idempotencia, referencias y conservación de KB anterior. No silencies conflictos, no edites generados a mano, no marques reglas implementadas por su mera promoción. Entrega evidencias y libera archivos al coordinador; no hagas commit o push por iniciativa propia.

## Prompt de revisión

> Revisa T07 contra inventarios aprobados y previsualización. Comprueba todas las bajas, fusiones, referencias, cobertura de esquemas 2B e idempotencia. Busca pérdida de variantes y sustitución incorrecta de precios de mercado por precios de banda. Verifica que staging no se carga en ejecución y que los tests adaptados conservan garantías. Comunica al coordinador hallazgos concretos y comandos independientes ejecutados.

## Prompt de reanudación

> Retoma T07 desde su última evidencia. Comprueba si la promoción ya se aplicó antes de ejecutarla nuevamente; usa previsualización y diff. Reconfirma propiedad exclusiva y revisiones de entradas. Resuelve solo conflictos/pruebas pendientes, conserva cambios ajenos y entrega el estado exacto de conservación e idempotencia al coordinador.
