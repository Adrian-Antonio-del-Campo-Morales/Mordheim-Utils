{
 "plan": "web-test-migration-plan.md §T2",
 "kind": "shared-parity-vectors",
 "source_family": "tests/campaign/test_rules_catalogue.py",
 "source_tests": 10,
 "gap": {
  "id": "artefact-lacks-prose-catalogue",
  "discovered_by": "vector cross-validation (T5 flow)",
  "description": "The web KB artefact carries rule_ids but not the browsable rules prose catalogue (documents, names, effects, tags, ES i18n) that the desktop RULES browser reads from sources/knowledge/catalog/rules/*.yaml via mordheim_knowledge. The generator (tools/knowledge/generate_knowledge_web.py) has never embedded it. Blocked vectors stay here with status=blocked so the completeness gate keeps them visible; unblocking = extend the generator with a `rules_prose` section, then flip status to ready and the TS mirror can assert them.",
  "owner": "unassigned",
  "blocked_vectors": 8
 },
 "rules": [
  "assertions run against the SAME generated KB artefact in both toolchains",
  "status=ready vectors execute now; status=blocked vectors document parity gaps and must not be silently dropped"
 ],
 "vectors": [
  {"id": "rules.wrong-ruleset-rejected", "status": "ready", "input": {"ruleset": "not-mordheim"}, "expect": {"error": true}},
  {"id": "rules.search-empty-and-miss", "status": "ready", "input": {"query": ""}, "expect": {"hits": 0}},
  {"id": "rules.prose-documents-present", "status": "blocked", "expect": {"prose_stems_include": ["special-rules", "conditions", "core-combat"]}},
  {"id": "rules.special-rules-count", "status": "blocked", "expect": {"min_special_rules": 90}},
  {"id": "rules.categories-ordered", "status": "blocked", "expect": {"first_category": "special-rules", "must_include": ["conditions", "skills", "equipment", "spells", "scenarios", "injuries"]}},
  {"id": "rules.always-hungry-en", "status": "blocked", "expect": {"entry_id": "shared-rule.always-hungry", "name": "Always Hungry", "effect_prefix": "A Troll requires"}},
  {"id": "rules.fires-of-uzhul", "status": "blocked", "expect": {"entry_id": "spell.lesser-magic.fires-of-uzhul", "name": "Fires of U'Zhul", "any_tag_contains": "difficulty 7"}},
  {"id": "rules.always-hungry-es", "status": "blocked", "expect": {"entry_id": "shared-rule.always-hungry", "locale": "es", "name": "Siempre Hambriento"}},
  {"id": "rules.search-accents", "status": "blocked", "input": {"query": "always hungry"}, "expect": {"hits_include": "shared-rule.always-hungry"}},
  {"id": "rules.search-scoped", "status": "blocked", "input": {"query": "frenzy", "category_id": "conditions"}, "expect": {"all_categories": "conditions"}}
 ]
}
